# Ditoo

Terminal controller for Divoom Ditoo-Plus pixel art speaker.

## Usage

```bash
ditoo                           # Launch TUI
ditoo --generate-config         # Print example config
ditoo --mac AA:BB:CC:DD:EE:FF   # Override device MAC
```

## Configuration

Save to `~/.config/ditoo/config.toml`
